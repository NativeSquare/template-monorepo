{
  "name": "@packages/backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "convex dev",
    "setup": "convex dev --until-success",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "typecheck": "tsc --noEmit -p convex",
    "migrate": "convex run migrations:runAll",
    "migrate:dryrun": "convex run migrations:runAll \"{dryRun: true}\"",
    "migrate:status": "convex run --component migrations lib:getStatus",
    "deploy:preview": "dotenv -e .env.preview -- convex deploy --preview-create preview",
    "deploy:prod": "convex deploy --prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@auth/core": "0.37.0",
    "@convex-dev/auth": "^0.0.90",
    "@convex-dev/migrations": "^0.3.5",
    "@convex-dev/resend": "^0.2.4",
    "@packages/shared": "workspace:*",
    "@packages/transactional": "workspace:*",
    "convex": "^1.40.0",
    "oslo": "^1.2.1",
    "resend": "6.12.4"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/react": "~19.1.0",
    "convex-test": "^0.0.53",
    "dotenv-cli": "^11.0.0",
    "typescript": "5.9.3",
    "vitest": "^4.1.8"
  }
}
